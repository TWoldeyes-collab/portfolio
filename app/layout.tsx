import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Tey - Data Engineer Portfolio",
  description: "Portfolio of a Data Engineer specializing in Azure, Python, PySpark, and SQL",
  keywords: ["Data Engineer", "Azure", "Python", "PySpark", "SQL", "C#", "Microsoft Fabric"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}